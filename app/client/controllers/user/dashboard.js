'use strict';

angular
    .module('klarifyApp')
    .controller(
        'dashboardCtrl',
        [
            '$scope',
            'Auth',
            '$http',
            'toaster',
            function ($scope, Auth, $http, toaster) {

            }
        ]);
