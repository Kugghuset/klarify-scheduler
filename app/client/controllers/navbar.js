'use strict';

angular
    .module('klarifyApp')
    .controller(
        'navbarCtrl',
        [
            '$scope',
            function($scope) {
                console.log('navbar ctrl');
            }
        ]);
