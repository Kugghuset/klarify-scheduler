'use strict';

angular
    .module('klarifyApp')
    .controller(
        'endpointsCtrl',
        [
            '$scope',
            function ($scope) {

                $scope.createEndpoint = function () {

                }
            }
        ]);
