'use strict';

angular
    .module('klarifyApp')
    .controller(
        'dashboardCtrl',
        [
            '$scope',
            'Auth',
            '$rootScope',
            function ($scope, Auth, $rootScope) {
            }
        ]);
